import styled from "styled-components";

const StyledCurrentTitle = styled.div`
    font-weight: 300;
    font-size: 40px;
    display: flex;
    align-items: center;
    letter-spacing: 0;
    margin-bottom: 24px;

    img {
        margin-right: 11px;
    }
`;

export default StyledCurrentTitle;
