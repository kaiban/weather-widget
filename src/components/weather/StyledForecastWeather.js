import styled from "styled-components";

const StyledForecastWeather = styled.div`
    display: flex;
    align-items: stretch;
    justify-content: space-between;
    flex-wrap: wrap;
    padding: 16px 16px 0;
`;

export default StyledForecastWeather;
