import styled from "styled-components";

const StyledWeatherCardIcon = styled.img`
    width: 100%;
    max-width: 144px;

    &.sun {
        width: 80%;
    }
`;

export default StyledWeatherCardIcon;
